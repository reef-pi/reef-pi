# Claude Code prompt — Issue #28: Material-UI v4 exit plan

You are Claude Code, running inside the **reef-pi Design System** project.

## Goal
Retire `@material-ui/core` (v4, EOL) from reef-pi by replacing `<Switch>` / `<FormControlLabel>` with the E2 `<ToggleSwitch>` + `<Field>` primitives. **Do not migrate to MUI v5/v6.**

## Source of truth
Open and follow exactly:
- `github_issues/issue-28-mui-exit.md` — replacement table, route checklist, acceptance.
- `github_issues/epic-06-framework-exit.md` — parent rationale.
- `github_issues/issue-15-toggle-states.md` — `<ToggleSwitch>` API.
- `github_issues/CLAUDE.md` — definition of done, file conventions.
- `SKILL.md` — design system rules.

## Operating mode
This is a **one-route-per-PR migration**. Each invocation handles **exactly one of**:
1. Initial audit (`git grep` map of every MUI import in `front-end/src/`).
2. One route migrated.
3. Final deletion PR (only when every route is ticked).

Ask the user which slice before starting. Default to "next unticked route in the issue checklist."

## Constraints (non-negotiable)
- One PR per route.
- No MUI v5/v6 import. No new icon library; inline SVG or `react-icons` for any MUI icon used.
- All new color usage via `var(--reefpi-*)`. No hex literals.
- Tap targets ≥ 44px.
- Visual parity: relay toggles must look identical to production except for the new pending/error states from #15. Screenshot diff in the PR body.
- If `<Field>` primitive doesn't exist yet, **stop and file the sub-issue**. Do not invent it inline.

## Plan
Before writing code:
1. State which route you're migrating.
2. `git grep "@material-ui" front-end/src/<route>` — list every occurrence.
3. List every file you will touch.
4. Confirm, then implement.

## Done
- Route ticked in `issue-28-mui-exit.md`.
- `BACKLOG.md` updated if this completes the parent issue.
- PR title format: `[claude design] route/<name>: off Material-UI`.
- PR body shows `git grep "@material-ui" front-end/src/<route>` returning 0 matches.
- Closing PR (when last route ships) removes `@material-ui/core`, `@material-ui/icons`, `@material-ui/styles`, and `react-toggle-switch` from `package.json` and documents the bundle-size delta.
