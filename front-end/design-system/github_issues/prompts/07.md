# Claude Code prompt — Issue #7: RangeSelector (1H / 6H / 1D / 7D / 30D)

You are Claude Code, running inside the **reef-pi Design System** project.

## Goal
RangeSelector (1H / 6H / 1D / 7D / 30D)

## Source of truth
Open and follow exactly:
- `github_issues/issue-07-range-selector.md` — full spec, API, visual, acceptance.
- `github_issues/CLAUDE.md` — definition of done, file conventions.
- `SKILL.md` — design system rules (single brand green, no emoji, 44px tap target, Questrial only).
- `github_issues/STRATEGY.md` — sequencing + flag policy.

## Constraints (non-negotiable)
- One PR per issue. No bundling.
- No new accent colors. No new fonts. Navbar is the only gradient.
- All new color usage via `var(--reefpi-*)`. No hex literals in CSS.
- Tap targets ≥ 44px on every interactive element.
- If you discover this needs a backend/API change and the issue isn't labeled `needs: api`, **stop** and file a follow-up issue instead of improvising.
- If the issue says "ship behind a flag", ship behind a flag — default off until parent epic is green.

## Plan
Before writing code:
1. Echo the acceptance checklist from `issue-07-range-selector.md` in your reply.
2. List every file you intend to create or modify.
3. Confirm the plan, then implement.

## Done
When all acceptance items pass:
- Tick the matching row in `github_issues/BACKLOG.md`.
- Update the relevant `preview/` card if a token or component changed.
- Commit with message: `[claude design] RangeSelector (1H / 6H / 1D / 7D / 30D)`
- Open a PR titled the same; in the body, link the parent epic from `github_issues/STRATEGY.md`.
