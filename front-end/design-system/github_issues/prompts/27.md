# Claude Code prompt — Issue #27: Bootstrap 4.6 exit plan

You are Claude Code, running inside the **reef-pi Design System** project.

## Goal
Retire Bootstrap 4.6 from reef-pi — utilities, then controls, then JS. **Do not migrate to Bootstrap 5.** Do not introduce Tailwind. The destination is `var(--reefpi-*)` + E2 primitives + plain CSS.

## Source of truth
Open and follow exactly:
- `github_issues/issue-27-bootstrap-exit.md` — three-tier plan, route checklist, acceptance.
- `github_issues/epic-06-framework-exit.md` — parent rationale + scope.
- `github_issues/CLAUDE.md` — definition of done, file conventions.
- `SKILL.md` — design system rules.
- `github_issues/STRATEGY.md` — sequencing + flag policy.

## Operating mode
This is a **long-running migration**, not a single PR. Each invocation of this prompt should handle **exactly one of**:
1. Tier 1 lint rule + one round of grid/utility cleanup across new code.
2. One route (Tier 2) migrated off `.btn-*` / `.form-control` / `.list-group-*`.
3. One Bootstrap JS replacement (Tier 3): modal → `<Dialog>`, dropdown → `<Menu>`, etc.
4. The final deletion PR (only when the issue's acceptance checklist is fully green).

Before doing anything, ask the user: **which tier and which slice are we shipping in this PR?** If unclear, default to "next unmigrated route from the issue checklist."

## Constraints (non-negotiable)
- One PR per slice. No combining tier 1 + tier 2 + tier 3 in one diff.
- No new CSS framework. No Tailwind, no Bootstrap 5, no Bulma.
- All new color usage via `var(--reefpi-*)`. No hex literals.
- Tap targets ≥ 44px.
- If a route has functional bugs you discover mid-migration, **stop and file a separate issue**. Do not fix in the migration PR.
- If an E2 primitive needed for the migration doesn't exist yet (e.g. `<Field>`), **stop and file a sub-issue**. Do not invent it inline.

## Plan
Before writing code:
1. State which tier and slice you're shipping.
2. List every file you will touch (use `git grep` output for proof of scope).
3. Show the `before` Bootstrap-class count for the slice.
4. Confirm, then implement.

## Done
- The route/tier ticked in `issue-27-bootstrap-exit.md`.
- `BACKLOG.md` updated if this slice completes the parent issue.
- Updated `preview/` cards if any token or primitive changed.
- PR title format: `[claude design] route/<name>: off Bootstrap` (Tier 2) or `[claude design] Bootstrap exit · tier N · <thing>` (Tier 1/3).
- PR body shows `git grep` proof of 0 Bootstrap classes in the touched files.
- Closing PR (when last slice ships) also removes `bootstrap` + `jquery` from `package.json` and the `@import` from `style.scss`, and documents the bundle-size delta.
